{
  "name": "2d-viewport",
  "version": "0.0.2",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "esbuild src/main.ts --bundle --outdir=dist --watch",
    "build": "esbuild src/main.ts --bundle --outdir=dist && cp -r dist docs && cp index.html docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "https://github.com/lowfront/2d-viewport",
  "bugs": {
    "url": "https://github.com/lowfront/2d-viewport/issues"
  },
  "author": {
    "name": "lowfront",
    "email": "lowfronterior@gmail.com"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.39"
  }
}
